%!TEX TS-program = pdflatex
%!TEX TS-options = -shell-escape
% % % % %   Die folgenden Zeilen müssen ihre Zeilennummern 4 und 5 behalten !!!    % % % % %
\newcommand{\printpraesenzlsg}{false}
\newcommand{\printloesungen}{false}
\newcommand{\printbewertungen}{false}
% % % % %   \newcommand{\printloesungen}{false}                                    % % % % %
\newcommand{\blattnummer}{1}
%\newcommand{\abgabetermin}{\textcolor{red}{bis 11.04.2022, 09:00 Uhr}}
\input{include/config.tex}
\usepackage{booktabs}

% Änderungen 2020: Hinweise auf Digitallehre angepasst; Rechnergeschichte entfernt; Blatt 1 und 2 zusammengefasst.
\begin{document}

\iforiginal{\input{include/kopf.tex}}
\aufgabetitel{$4$}{Mengen und Funktionen}\\
Gegeben seien die Mengen $A=\{\emptyset,\{a,b\},c\}$ und $B=\{\{c\},a\}$.
\begin{enumerate}[(a)]
\item Geben Sie die Menge $A\cup B$ an.
\item Geben Sie die Menge $A\cap B$ an.
\item Geben Sie die Menge $A\setminus B$ an.
\item Geben Sie $\mathscr{P}(B)$ an.

\end{enumerate}

\aufgabetitel{$10$}{Aussagen zu Mengen}\\
Gegeben sei die Menge $A=\{1,\{1\},\{1,2\},\{\emptyset\}\}$, $\mathscr{P}(A)$ die Potenzmenge von $A$. Bewerten Sie die folgenden Aussagen (jeweils ja oder nein angeben).
\begin{enumerate}[(a)]
\item $1\in A$
\item $\{1\}\in A$
\item $\{1\}\subseteq A$
\item $\{\{1\}\}\in A$
\item $\{\{1\}\}\subseteq A$
\item $\{1\}\in\mathscr{P}(A)$
\item $\emptyset\in A$
\item $\emptyset\in\mathscr{P}(A)$
\item $\{\emptyset\}\in\mathscr{P}(A)$
\item $\{\{\emptyset\}\}\in\mathscr{P}(A)$

\end{enumerate}

\aufgabetitel{$8$}{Relationen}\\
In einer Firma gibt es eine Matrixorganisation: Die Mitarbeiter sind $M=\{\text{David},\text{Eve},\text{Frank}, \text{Grace}\}$, die Vorgesetzten sind $V=\{\text{Anna},\text{Bob},\text{Charlie}\}$. Jeder Mitarbeiter hat genau einen direkten Vorgesetzten, kann aber auch an mehreren Projekten arbeiten. Die folgende Tabelle gibt eine Übersicht über die Mitarbeiter, ihre Vorgesetzten und die Projekte, an denen sie arbeiten.
\begin{table}[h]
    \centering
    \caption{Projektübersicht der Mitarbeiter}
    \begin{tabular}{lll}
        \toprule
        \textbf{Mitarbeiter} & \textbf{Vorgesetzte} & \textbf{Projekt} \\
        \midrule
        David & Anna    & Projekt 1 \\
        Eve    & Charlie & Projekt 1 und Projekt 2 \\
        Frank  & Anna    & Projekt 2 \\
        Grace  & Bob     & Projekt 2 und Projekt 3 \\
        \bottomrule
    \end{tabular}
\end{table}
Den Projekten sind aus der Menge der Vorgesetzten Koordinatoren zugeordnet:
\begin{table}[h]
    \centering
    \caption{Projekleitungen}
    \begin{tabular}{ll}
        \toprule
        \textbf{Projekt} & \textbf{Koordinatoren}\\
        \midrule
        Projekt 1 & Anna und Bob    \\
        Projekt 2 & Bob und Charlie    \\
        Projekt 3 & Charlie     \\
        \bottomrule
    \end{tabular}
\end{table}
Wir sind neu im Unternehmen und wollen f"ur unser eigenes Projekt ein gutes Stakeholder-Management aufbauen. Dazu wollen wir die Relationen zwischen den Mitarbeitenden und den Vorgesetzten analysieren. 
\begin{enumerate}[(a)]
    \item Stllen Sie die Vorgesetzten-Mitarbeiter-Relation $R\subseteq V\times M$ als Graphen und Adjazenzmatrix dar.
    \item Stellen Sie die Mitarbeiter-Projekt-Relation $S\subseteq M\times P$ als Graphen und Adjazenzmatrix dar.
    \item Stellen Sie die Projekt-Koordinatoren-Relation $T\subseteq P\times V$ als Graphen und Adjazenzmatrix dar.
    \item Wir wollen herausfinden, welche Vorgesetzten den gr"o{\ss}ten Einfluss haben. Dazu wollen wir diejenige Relation modellieren, die Vorgesetzte mit Vorgesetzte in Beziehung setzt. Die Relation soll ausdrücken, dass ein Vorgesetzter auf andere Vorgesetzte Einfluss über die Mitarbeiter und deren Projekte, an denen diese mitarbeiten und die er mit koordiniert, hat. Modellieren Sie die Relation mit Hilfe von $R$, $S$ und $T$, stellen Sie sie als Graphen dar und interpretieren Sie das Ergebnis.
\end{enumerate}


\aufgabetitel{$6$}{Bell-LaPadula}\\
In einem Hochsicherheitsbereich werden Sicherheitslabels verwendet. Ein Label besteht aus einem Sicherheitslevel und einer Menge von Kategorien (Need-to-Know-Prinzip). 
Die Menge der Labels ist definiert als $L=\text{Level}\times\mathcal{P}(\{A,B\})$, wobei $\{A,B\}$ die Menge der Kategorien (z.B. Abteilung $A$ oder $B$ ist.
Die Sicherheitslevel sind geordnet: $0$ (Public) $< 1$ (Secret).
Ein Label ist also ein Paar $(l,K)$. 

Wir definieren die Dominanzrelation $\preceq$ auf $L$ wie folgt: $(l_1,K_1) \preceq (l_2,K_2)\Leftrightarrow l_1 \leq l_2 \wedge K_1 \subseteq K_2$ gilt.
Die Labels k"onnen Subjekten (wie Usern) und Objekten (wie Dateien) zugewiesen werden.
Die Idee ist, dass ein Subjekt mit Label $L_s=(l_s,K_s)$ auf ein Objekt mit Label lesend $L_o=(l_o,K_o)$ zugreifen darf, wenn $L_o \preceq L_s$ gilt (no read up).
Gegeben seien folgende Sicherheitslabel:
\begin{itemize}
    \item $L_1 = (0,\emptyset)$
    \item $L_2 = (1,\{A\})$
    \item $L_3 = (1,\{B\})$
    \item $L_4 = (1,\{A,B\})$
    \item $L_5 = (0,\{A\})$
   
\end{itemize}
\begin{enumerate}[(a)]
    \item Zeichnen Sie das Hasse-Diagramm der Labels bez"uglich der Dominanzrelation $\preceq$ f"uer die Labels $L_1$ bis $L_5$.
    \item Ein Benutzer Alice hat das Label $L_A = (1,\{A\})$. Darf Alice auf Objekte mit den Labels $L_1$ bis $L_5$ lesend zugreifen? Begr"unden Sie Ihre Antwort.
\end{enumerate}

\aufgabetitel{$5$}{Beweis mit Relationen}\\
Sei $M$ eine nichtleere Menge, $R,S\subseteq M\times M$ zwei Relationen auf $M$. Beweisen Sie, dass wenn $R$ und $S$ beide transitiv sind, auch die Relation $R\cap S$ transitiv ist.

\hspace{5cm}
\newpage
\underline{\textbf{Formelsammlung}}\\
Hier eine kleine Formelsammlung. Sie ist nicht vollständig, enthält aber alle wichtigen Statements / Definitionen, die man brauchen könnte.
\begin{enumerate}
\item Aussagen- und Pr"adikatenlogik
\begin{enumerate}
    \item Distributivgesetz: $A\wedge (B \vee C) \Leftrightarrow (A\wedge B) \vee (A\wedge C)$
    \item Distributivgesetz: $A\vee (B \wedge C) \Leftrightarrow (A\vee B) \wedge (A\vee C)$
    \item DeMorgan: $\neg(A\wedge B) \Leftrightarrow \neg A \vee \neg B$
    \item DeMorgan: $\neg(A\vee B) \Leftrightarrow \neg A \wedge \neg B$
    \item Idempotenz: $A\wedge A \Leftrightarrow A$
    \item Idempotenz: $A\vee A \Leftrightarrow A$
    \item $A\wedge \neg A \Leftrightarrow \bot$
    \item $A\vee \neg A \Leftrightarrow \top$
    \item $\neg\neg A \Leftrightarrow A$
    \item $\neg\forall x\in M: A(x) \Leftrightarrow \exists x\in M: \neg A(x)$
    \item $\neg\exists x\in M: A(x) \Leftrightarrow \forall x\in M: \neg A(x)$
\end{enumerate}
\item Mengen
\begin{enumerate}
    \item Teilmenge: $A\subseteq B \Leftrightarrow \forall x\in A: x\in B$
    \item Potenzmenge: $\mathscr{P}(A) = \{B\mid B\subseteq A\}$
    \item Vereinigung: $A\cup B = \{x\mid x\in A \vee x\in B\}$
    \item Schnittmenge: $A\cap B = \{x\mid x\in A \wedge x\in B\}$
    \item Differenzmenge: $A\setminus B = \{x\mid x\in A \wedge x\notin B\}$
    \item Distributivgesetz: $A\cap (B \cup C) \Leftrightarrow (A\cap B) \cup (A\cap C)$
    \item Distributivgesetz: $A\cup (B \cap C) \Leftrightarrow (A\cup B) \cap (A\cup C)$
    \item DeMorgan: $A\setminus (B\cup C) \Leftrightarrow (A\setminus B) \cap (A\setminus C)$
    \item DeMorgan: $A\setminus (B\cap C) \Leftrightarrow (A\setminus B) \cup (A\setminus C)$
    \item Es ist $\bigcup_{i\in I}A_i = \{x\mid \exists i\in I: x\in A_i\}$. 
    \item Es ist $\bigcap_{i\in I}A_i = \{x\mid\forall i\in I: x\in A_i\}$.
\end{enumerate}
\item Relationen
\begin{enumerate}
    \item F"ur Mengen $M,N$ ist $R\subseteq M\times N$ eine Relation von $M$ nach $N$.
    \item $R\subseteq M\times M$ ist reflexiv, wenn $\forall x\in M: (x,x)\in R$.
    \item $R\subseteq M\times M$ ist symmetrisch, wenn $\forall x,y\in M: (x,y)\in R \Rightarrow (y,x)\in R$.
    \item $R\subseteq M\times M$ ist antisymmetrisch, wenn $\forall x,y\in M: (x,y)\in R \wedge (y,x)\in R \Rightarrow x=y$.
    \item $R\subseteq M\times M$ ist transitiv, wenn $\forall x,y,z\in M: (x,y)\in R \wedge (y,z)\in R \Rightarrow (x,z)\in R$.
    \item $R\subseteq M\times M$ ist eine "Aquivalenzrelation, wenn $R$ reflexiv, symmetrisch und transitiv ist.
    \item $R\subseteq M\times M$ ist eine Ordnungsrelation, wenn $R$ reflexiv, antisymmetrisch und transitiv ist.
    \item F"ur eine "Aquivalenzrelation $\sim$ auf $M$ ist $[x]=\{y\in M\mid x\sim y\}$ die "Aquivalenzklasse von $x$, $M/\sim = \{[x]\mid x\in M\}$ die Menge der "Aquivalenzklassen oder Quotientenmenge von $M$ modulo $\sim$. Die Menge der "Aquivalenzklassen ist eine Partition von $M$. Umgekehrt induziert jede Partition eine "Aquivalenzrelation.
    \item F"ur eine Ordnungsrelation $\leq$ auf $M$ und $X\subseteq M$ ist $g$ ein kleinstes Element von $X$, wenn $\forall x\in X: g\leq x$, $g$ ein minimales Element von $X$, wenn $\forall g'\in X: g'\leq g\Rightarrow g=g'$, maximale und gr"o{\ss}te Elemente analog.
    \item Es ist $R^*$ die reflexiv-transitive H"ulle f"ur eine Relation $R$.
\end{enumerate}
\item Funktionen
\begin{enumerate}
    \item Eine Funktion $f:X\to Y$ ist eine Relation (also $f\subseteq X\times Y$), die jedem Element aus der Definitionsmenge $X$ genau ein Element aus der Zielmenge $Y$ zuordnet.
    \item $f$ ist injektiv, wenn $\forall x_1,x_2\in X: f(x_1)=f(x_2) \Rightarrow x_1=x_2$.
    \item $f$ ist surjektiv, wenn $\forall y\in Y: \exists x\in X: f(x)=y$.
    \item $f$ ist bijektiv, wenn $f$ injektiv und surjektiv ist.
    \item Die Umkehrfunktion $f^{-1}$ ist definiert $f^{-1}(Y)=\{x\in X\mid\exists y\in Y: y=f(x)\}$
\end{enumerate}
\end{enumerate}
\end{document}